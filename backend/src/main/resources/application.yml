spring:
  application:
    name: 'AirfareCalculationService'

  datasource:
    url: 'jdbc:postgresql://localhost:5432/airfare_calc_service_db'
    username: 'postgres'
    password: 'pass'

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: update

server:
  port: 8000